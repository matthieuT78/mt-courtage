User-agent: *
Allow: /

# Espaces privés ou techniques
Disallow: /admin
Disallow: /admin/
Disallow: /api/

# Les pages privées HTML restent crawlables afin que Google lise leur balise noindex.
# Elles ne sont pas proposées dans le sitemap.

# Les pages /simulateur/... restent crawlables pour que Google voie le noindex,
# mais elles ne sont plus poussées dans le sitemap.

Sitemap: https://lokt.fr/sitemap.xml
